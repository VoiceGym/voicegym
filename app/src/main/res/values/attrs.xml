<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="SpectrogramView">
        <attr name="top_margin" format="float"/>
        <attr name="bottom_margin" format="float"/>
        <attr name="left_margin" format="float"/>
        <attr name="right_margin" format="float"/>
        <attr name="draw_border" format="boolean"/>
        <attr name="draw_scale" format="boolean"/>
        <attr name="border_thickness" format="float"/>
        <attr name="color_map" format="enum">
            <enum name="hot_map" value="0"/>
        </attr>
    </declare-styleable>
</resources>
